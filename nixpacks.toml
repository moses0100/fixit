# ตั้งเฉพาะคำสั่ง start ให้ nixpacks ตรวจจับ setup/install/build เอง
# เพื่อให้ extension จาก composer.json (เช่น ext-gd) ถูกติดตั้งโดย provider
[start]
cmd = "php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=$PORT"
